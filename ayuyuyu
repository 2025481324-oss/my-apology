<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Favorite Person 💙</title>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --soft-blue: #e0f2fe;
            --main-blue: #7dd3fc;
            --text-color: #0369a1;
        }

        body {
            background-color: var(--soft-blue);
            font-family: 'Quicksand', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: var(--text-color);
            overflow: hidden;
        }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            text-align: center;
            max-width: 400px;
            width: 90%;
            border: 4px solid var(--main-blue);
        }

        .rimuru-slime {
            width: 100px;
            margin-bottom: 1rem;
            animation: bounce 2s infinite ease-in-out;
        }

        h1 { font-size: 1.5rem; margin-bottom: 1rem; }

        .btn-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
            position: relative;
        }

        button {
            padding: 10px 25px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
            transition: transform 0.2s;
        }

        #yesBtn { background-color: #93c5fd; color: white; }
        #noBtn { background-color: #fda4af; color: white; position: absolute; left: 160px;}

        .hidden { display: none; }

        .letter {
            line-height: 1.6;
            text-align: left;
            font-size: 0.9rem;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
    </style>
</head>
<body>

    <div class="card" id="mainCard">
        <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExNHJ1ZzJyeW94bmR6eXN6eHh4eHh4eHh4eHh4eHh4eHh4eHh4eHh4eCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9cw/vXjY892v3GqJ7H6RzP/giphy.gif" alt="Cute Slime" class="rimuru-slime">
        
        <div id="questionArea">
            <h1>Will you forgive your favorite person? 🥺</h1>
            <p>I'm really sorry for everything, please don't be mad at me anymore...</p>
            <div class="btn-container">
                <button id="yesBtn" onclick="showLetter()">Yes!</button>
                <button id="noBtn" onmouseover="moveButton()">No</button>
            </div>
        </div>

        <div id="letterArea" class="hidden">
            <h2 style="color: #0ea5e9;">To My Amazing Girl 💙</h2>
            <div class="letter">
                <p>I know I've been a bit of a handful lately, but like Rimuru, I'm always evolving to be better for you! You’re the Okarun to my Momo (or vice versa!), and I hate it when things aren't right between us.</p>
                <p>I miss your smile and your sweetness. Can we go back to being us again? I love you more than all the anime in the world!</p>
            </div>
            <p><b>I love you so much!</b></p>
        </div>
    </div>

    <script>
        function moveButton() {
            const btn = document.getElementById('noBtn');
            const x = Math.random() * (window.innerWidth - btn.offsetWidth);
            const y = Math.random() * (window.innerHeight - btn.offsetHeight);
            
            btn.style.position = 'fixed';
            btn.style.left = x + 'px';
            btn.style.top = y + 'px';
        }
function showLetter() {
            document.getElementById('questionArea').classList.add('hidden');
            document.getElementById('letterArea').classList.remove('hidden');
            // Stop the No button from floating around
            document.getElementById('noBtn').classList.add('hidden');
        }
    </script>
</body>
</html>
